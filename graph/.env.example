# Embedded engine (default) — no server needed.
GRAPH_BACKEND=kuzu
KUZU_PATH=.kuzu/gerd.kz

# Server engine — docker compose up -d, then flip GRAPH_BACKEND to neo4j.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=gerdgraph
NEO4J_DATABASE=neo4j
